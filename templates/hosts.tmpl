[primary]
${ primary.ip } new_hostname=${ primary.name }

[secondary]
%{ for i in secondary ~}
${ i.ip } new_hostname=${ i.name }
%{ endfor ~}

[control:children]
primary
secondary

[control:vars]
nginx_ip=${ nginx.ip }

[worker]
%{ for i in workers ~}
${ i.ip } new_hostname=${ i.name }
%{ endfor ~}

[worker:vars]
nginx_ip=${ nginx.ip }

[haproxy]
${ nginx.ip } new_hostname=${ nginx.name }